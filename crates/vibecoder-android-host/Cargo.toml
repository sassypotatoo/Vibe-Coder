[package]
name = "vibecoder-android-host"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lib]
crate-type = ["rlib", "cdylib"]

[dependencies]
serde.workspace = true
serde_json.workspace = true
tokio.workspace = true
libc.workspace = true
sha2.workspace = true
uuid.workspace = true
vibecoder-core = { path = "../vibecoder-core" }
vibecoder-persistence-local = { path = "../vibecoder-persistence-local" }
vibecoder-checkpoint-local = { path = "../vibecoder-checkpoint-local" }
vibecoder-workspace-local = { path = "../vibecoder-workspace-local" }
vibecoder-gateway-contract = { path = "../vibecoder-gateway-contract" }
vibecoder-gateway-omniroute = { path = "../vibecoder-gateway-omniroute" }
vibecoder-agent-jcode = { path = "../vibecoder-agent-jcode" }
vibecoder-domain = { path = "../vibecoder-domain" }
vibecoder-process-contract = { path = "../vibecoder-process-contract" }
vibecoder-process-local = { path = "../vibecoder-process-local" }
vibecoder-runtime-packaging = { path = "../vibecoder-runtime-packaging" }
vibecoder-routing = { path = "../vibecoder-routing" }
